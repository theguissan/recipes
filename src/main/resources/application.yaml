server:
  port: 9096

spring:
  datasource:
    url: jdbc:postgresql://db:5433/CMP1611-matheus-ana-ismael
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        ddl-auto: none
        temp:
          use_jdbc_metadata_defaults: false
    show-sql: true



